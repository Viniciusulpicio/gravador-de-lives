services:
  - type: cron
    name: gravador-de-lives-nerd
    plan: free
    dockerfilePath: Dockerfile
    schedule: "0 21 * * 5"
    envGroups: # <--- ADICIONE ISTO
      - name: gravador-secrets # <--- USE O NOME DO GRUPO QUE VOCÊ CRIOU
    secretFiles:
      - key: drive_config
        path: /rclone-secrets/drive_config
