name: Teste Simples

on:
  workflow_dispatch:

jobs:
  teste:
    runs-on: ubuntu-latest
    steps:
      - name: Imprimir Mensagem
        run: echo "Olá Mundo! O workflow de teste está a funcionar."

